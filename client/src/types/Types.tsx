export type IntegrationsAi = {
  id: string;
  title: string;
  icon: string;
};

export type ProcessBlurb = {
  id: string;
  title: string;
  description: string;
  icon: string;
};

export type NavItem = {
  label: string;
  href: string;
  icon: string;
};
