import { allIntegrations } from "../../data/landingPage";

const Integrations = () => {
  return (
    <section className="bg-transparent mx-auto flex justify-around items-center"></section>
  );
};

export default Integrations;
